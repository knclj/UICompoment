<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.uicompoment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UICompoment">
        <activity android:name=".FlexBoxActivity"></activity>
        <activity android:name="com.test.touchevent.SRL_VP_main"></activity>
        <activity
            android:name=".FlowLayoutActity"
            android:label="@string/title_activity_flow_layout_actity"
            android:theme="@style/Theme.UICompoment.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>